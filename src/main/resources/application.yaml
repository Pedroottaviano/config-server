spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pedroottaviano/config-server
          default-label: main

server:
  port: 8888
